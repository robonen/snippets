import { defineLayerConfig } from 'vite-layers';

/**
 * Финансы как СЛОЙ. Слой — не пакет-обёртка: приложение наследует его
 * файлы, и любой из них можно перекрыть, положив свой по тому же пути слоем
 * выше. `package.json` рядом остаётся ради резолва зависимостей и своих гейтов —
 * слои и пакеты решают разные задачи и не конкурируют.
 */
export default defineLayerConfig({
  name: 'finance',
});
