/**
 * Идентификатор записи.
 *
 * UUID, а не счётчик: ключи рождаются на нескольких устройствах сразу и
 * съезжаются слиянием ленда. Счётчик там дал бы двум разным задачам один ключ —
 * и они склеились бы в одну молча.
 */
export function newId(): string {
  return crypto.randomUUID();
}
