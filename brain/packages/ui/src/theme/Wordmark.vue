<script setup lang="ts">
/**
 * Словесный знак: «brain» дисплейной «Unbounded» в рост графического знака.
 *
 * Гарнитура и есть растяжение: «Unbounded» — широкий гротеск, ему не нужен
 * дополнительный трекинг (на крупном кегле разрядка читалась бы как дыры).
 * Третьего шрифта у бренда нет: то же начертание, что у крупных чисел.
 *
 * Вес 400 — под плотность знака рядом: металлические ленты весомые, и лёгкое
 * 300 рядом с ними выглядело бы случайной парой.
 *
 * Строчные, не капс: слово начинает предложения интерфейса («brain держит
 * данные у вас»), и фирменный капс превращал бы каждое такое место в крик.
 */
const { size = '1.625rem' } = defineProps<{
  /** Кегль. По умолчанию — в рост знака в боковой панели (26 px). */
  size?: string;
}>();
</script>

<template>
  <span
    class="text-display leading-none font-normal text-text select-none"
    :style="{ fontSize: size }"
  >brain</span>
</template>
